{
  "name": "energy-moneysaver-web",
  "private": true,
  "version": "0.2.0-m1",
  "type": "module",
  "scripts": {
    "build:data": "node scripts/build-data.mjs",
    "predev": "npm run build:data",
    "prebuild": "npm run build:data",
    "pretest": "npm run build:data",
    "dev": "vite",
    "build": "tsc -p tsconfig.json && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "js-yaml": "^4.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11",
    "vitest": "^2.1.5"
  }
}
